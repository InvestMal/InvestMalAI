# InvestMalAI

Telegram bot with OpenAI integration — ready for Render.

## Setup (Render)
- Add environment variables: TELEGRAM_TOKEN, WEBHOOK_URL, OPENAI_API_KEY, PORT (optional)
- Deploy on Render (Web Service) with start command: `python bot.py`

Commands:
- /start
- /help
- /ai <prompt>
